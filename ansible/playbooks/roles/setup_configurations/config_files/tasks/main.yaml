# source $ZSH/oh-my-zsh.sh
- name: Source $HOME/.zprofile
  ansible.builtin.lineinfile:
    path: "{{item}}"
    insertbefore: 'source $ZSH/oh-my-zsh.sh'
    state: present
    line: |
      source $HOME/.zprofile
  loop:
    - "{{ansible_env.HOME}}/.zshrc"
